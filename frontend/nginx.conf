server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — everything that isn't a real file is the app shell.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
